# ------------------------------------
# Development Stage - 專為 Vite HMR 設計
# ------------------------------------
FROM node:20-alpine 

# 設定工作目錄
WORKDIR /app

# 複製 package 檔案，以便 Docker 利用快取
COPY package*.json ./

# 安裝所有依賴
RUN npm install --force

# 複製所有專案檔案 (包含 src, vite.config, tsconfig 等)
COPY . . 

# 容器啟動命令在 docker-compose.yml 中定義